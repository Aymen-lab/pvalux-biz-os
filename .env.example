# Lovable Cloud / Supabase — values are auto-injected by Lovable.
# This file documents the required variables; do not commit real secrets.

# Client (Vite-exposed)
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-anon-key"
VITE_SUPABASE_PROJECT_ID="your-project-ref"

# Server (do not prefix with VITE_)
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_PUBLISHABLE_KEY="your-publishable-anon-key"
# SUPABASE_SERVICE_ROLE_KEY is managed via Lovable secrets — never commit.
